{
  "name": "outlook-mail-manager",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:web\"",
    "dev:server": "cd server && npm run dev",
    "dev:web": "cd web && npm run dev",
    "build": "cd web && npm run build",
    "start": "cd server && npm start",
    "install:all": "npm install && cd server && npm install && cd ../web && npm install",
    "import:outlook": "node scripts/import-outlook-accounts.mjs",
    "test:import-outlook": "node --test scripts/import-outlook-accounts.test.mjs"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
